name: CI

on:
  pull_request:
  push:
    branches-ignore:
      - master
  workflow_call:

jobs:
  backend:
    name: Backend build and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      # Name the solution: backend/ holds both AltusIQ.sln and AltusIQ.Api.csproj,
      # and a bare dotnet command there fails as ambiguous.
      - name: Restore
        run: dotnet restore AltusIQ.sln
        working-directory: backend

      - name: Build
        run: dotnet build AltusIQ.sln --configuration Release --no-restore
        working-directory: backend

      - name: Test
        run: dotnet test AltusIQ.sln --configuration Release --no-build
        working-directory: backend

  frontend:
    name: Frontend lint and build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install
        run: npm ci
        working-directory: frontend

      - name: Lint
        run: npm run lint
        working-directory: frontend

      - name: Test
        run: npm test
        working-directory: frontend

      - name: Build
        run: npm run build
        working-directory: frontend
